#include <stdio.h>
#include <stdlib.h>

int main(int argc, char**argv)
{
    int tableau[4] = {0,0,0,0}, i = 0;  //initialisation du tableau

    for(i=0, i < 4, i++)
    {
        printf("‰d\n", tableau[i]);
    }
    return 0;
}